function Range(start, end, text, id, entityId){
    return (
        {
            start: start,
            end: end,
            text: text,
            id: id,
            entityId: entityId
        }
    )
}

export default Range;